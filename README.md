# itue301-exam-24CE027-B1-

# QuickBite Food Ordering System

## MongoDB Setup
1. Create a MongoDB database (Atlas or Local).
2. Copy `.env.example` to `.env`.
3. Add your connection string to `MONGO_URI`.

## How to Run Backend
```bash
cd backend
npm install
npm start
